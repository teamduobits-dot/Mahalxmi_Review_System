fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
itsdangerous==2.2.0
# Cloud backend (Firestore + Firebase Storage). Not needed for local SQLite
# mode — the app only imports firebase-admin when BACKEND_MODE=firebase.
firebase-admin>=6.5,<7
